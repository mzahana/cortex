# ------------------------------------------------------------------------------
# Dev/CI-only dependencies (T0.8): lint, type-check, test. NEVER installed in
# the deployed image (docker/Dockerfile only installs base.txt) — keeps the
# runtime image minimal per docs/deployment.md's RAM/disk budget.
# ------------------------------------------------------------------------------
-r base.txt

# --- Lint / format -------------------------------------------------------------
ruff==0.7.4
black==24.10.0

# --- Type-check ------------------------------------------------------------
mypy==1.13.0
django-stubs[compatible-mypy]==5.1.1
djangorestframework-stubs==3.15.1

# --- Test --------------------------------------------------------------------
pytest==8.3.3
pytest-django==4.9.0
pytest-cov==5.0.0
factory-boy==3.3.1

# T4.5 label-PDF round-trip test only (`apps/labels/tests/
# test_label_pdf_qr_roundtrip.py`): rasterize the rendered PDF page back to
# an image (`pymupdf`, pure wheel, no system dependency) and decode the
# embedded QR from it (`opencv-python-headless`'s `QRCodeDetector`, also a
# self-contained wheel) to prove the FULL pipeline — not just the
# pre-render payload — actually round-trips. Deliberately NOT `pyzbar`
# (needs the system `libzbar0` shared library, which a bare `pip install`
# on a CI runner does not provide -- would pass locally on a dev machine
# that happens to have it and then fail confusingly in CI); both packages
# chosen here are self-contained wheels with no extra `apt`/`apk` step.
pymupdf==1.24.11
opencv-python-headless==5.0.0.93
