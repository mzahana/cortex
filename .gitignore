# --- Secrets / env ---
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# --- Python / Django ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
ENV/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
db.sqlite3
db.sqlite3-journal
backend/staticfiles/
backend/static_root/
backend/media/
backend/mediafiles/
*.log

# --- Node / Vite / frontend ---
node_modules/
frontend/dist/
frontend/build/
frontend/.vite/
frontend/coverage/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.eslintcache

# --- Docker / compose local state ---
docker/cortex/
docker/**/pgdata/
docker/**/media/
docker/**/redis-data/
*.pid
# Local-only compose overlay (e.g. a laptop's `nginx` host-port mapping for
# browser testing, docs/deployment-runbook.md §0a) -- never a NAS/prod
# concern, so never committed.
docker-compose.override.yml

# --- Editors / OS ---
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# --- CI / misc ---
*.tmp
*.bak

# --- T6.6 load test: session cookies (credential-like, throwaway/dev-only
# fixture data), never committed. Result CSVs ARE committed (just timing
# numbers) -- see tests/load/README.md.
tests/load/results/sessions.json
tests/load/.venv-load/

# Local browser-preview nginx conf (generated from docker/nginx/default.conf
# by docker-compose.local.yml's documented sed — never deployed).
docker/nginx/default.local.conf
